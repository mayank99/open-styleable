<template webc:root="override" :shadowrootmode="shadowrootmode || 'open'">
	<link
		webc:if="adoptstyles === 'inherit'"
		webc:keep
		rel="stylesheet"
		:href="getBundleFileUrl('css')"
	/>

	<!--- this is a webc slot, not an HTML slot --->
	<slot></slot>
</template>
