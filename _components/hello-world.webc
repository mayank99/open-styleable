<shadow-root adoptstyles="inherit">
	<p class="shadow">This is inside shadow DOM (rebeccapurple).</p>
	<shadow-slot></shadow-slot>
</shadow-root>

<p class="light">And this is outside (hotpink).</p>

<style>
	p.shadow {
		color: var(--shadow-color, rebeccapurple);
	}
	p.light {
		color: var(--light-color, hotpink);
	}
</style>
